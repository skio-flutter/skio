name: publish skio_usb_serial

# pub.dev automated publishing settings for skio_usb_serial must be:
#   repository:  skio-flutter/skio
#   tag pattern: skio_usb_serial-v{{version}}
#   environment: pub.dev (required)
on:
  push:
    tags:
      - 'skio_usb_serial-v[0-9]+.[0-9]+.[0-9]+*'

permissions:
  contents: read

jobs:
  publish:
    permissions:
      id-token: write # OIDC token for pub.dev; no stored credentials.
    uses: dart-lang/setup-dart/.github/workflows/publish.yml@6afc89df92d6eb3834022f73cd65adc8cdfcb92d # v1.8.1
    with:
      environment: pub.dev
      working-directory: packages/skio_usb_serial
